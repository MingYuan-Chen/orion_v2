# 專案結構樹狀圖 (Project Structure Tree)

本文件描述 `psc_orion` 專案的資料夾結構與主要檔案功能。

## 根目錄 (Root Directory)

-   **`main.py`**: 應用程式進入點 (Entry Point)。負責初始化應用程式、載入設定與啟動主視窗。
-   **`requirements.txt` / `requirements_with_versions.txt`**: Python 依賴套件清單。
-   **`build/`, `dist/`, `main.spec`**: PyInstaller 打包相關檔案與輸出目錄。

## `core/` - 核心邏輯層

包含應用程式的主要程式碼，依照功能模組分層。

-   **`models/`**: 資料模型與設備通訊層
    -   `serial_device_model.py`: 負責與硬體設備進行 Serial Port 通訊與狀態維護。

-   **`services/`**: 業務邏輯服務層 (Business Logic)
    -   `diagnostic_service.py`: 執行診斷測試流程的邏輯。
    -   `network_service.py`: 處理網路連線與狀態監控。
    -   `battery_monitor_service.py`: 電池狀態監控服務。
    -   `system_info_service.py`: 取得系統資訊 (CPU, Memory, U-Boot 等)。
    -   `hw_config_service.py`: 處理硬體配置檔載入與驗證。
    -   `platform_detection_service.py`: 自動偵測當前運行的硬體平台。

-   **`view_models/`**: 視圖模型層 (ViewModel)
    -   `device_view_model.py`: 連接 View 與 Model/Services，處理 UI 邏輯與狀態通知。

-   **`views/`**: 使用者介面層 (User Interface) - 基於 PySide6 (Qt)
    -   `main_view.py`: 主視窗框架。
    -   `diagnostic_view.py`: 診斷功能頁面。
    -   `stability_test_view.py`: 穩定性測試 (Ping Test) 頁面。
    -   `wifi_connection_view.py`: WiFi 掃描與選擇視窗。
    -   `battery_monitor_view.py`: 電池監控頁面。
    -   `system_info_view.py`: 系統資訊顯示頁面。
    -   `function_control_view.py`: 功能控制測試頁面。
    -   `hw_config_view.py`: 硬體配置檢視頁面。

-   **`workers/`**: 背景工作執行緒 (Background Workers)
    -   `led_worker.py`: 處理 LED 燈號控制的背景工作。
    -   `backlight_worker.py`: 處理背光控制的背景工作。

## `resources/` - 資源與設定檔

-   **`commands/`**: 設備指令定義 (JSON)
    -   依照平台分類 (e.g., `argo`, `athena`, `gemini`, `hydra`)，定義該平台支援的指令集與回應格式。

-   **`hw_config/`**: 硬體配置定義 (JSON)
    -   存放不同 SKU 或專案的硬體規格設定 (e.g., GPIO pin map, sensor configs)。

-   **`icons/`**: 應用程式圖示資源。
-   **`themes/`**: UI 主題樣式 (QSS/CSS)。

## `util/` - 通用工具

-   **`logger.py`**: Logging 設定與工具。
-   **`command_loader.py`**: 負責讀取並解析 `resources/commands` 下的指令檔。
